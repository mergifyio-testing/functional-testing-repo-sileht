pull_request_rules:
- actions:
    queue:
      method: squash
      name: default
  conditions:
  - base=20211207081200/test_queue_without_queue_conditions/main
  - check-success=continuous-integration/fake-ci
  name: Merge priority high
queue_rules:
- conditions: []
  name: default
