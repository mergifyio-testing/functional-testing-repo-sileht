pull_request_rules:
- actions:
    label:
      toggle:
      - CI:fail
  conditions:
  - base=20230223153851/test_label_toggle/main
  - check-failure=continuous-integration/fake-ci
  name: toggle labels
