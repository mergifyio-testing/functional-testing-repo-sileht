pull_request_rules:
- actions:
    label:
      toggle:
      - CI:fail
  conditions:
  - base=20250224125835/test_label_toggle/main
  - check-failure=continuous-integration/fake-ci
  name: toggle labels
