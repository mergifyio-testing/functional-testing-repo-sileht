pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20221221114211/test_queue_inplace_train_resetted/main
  - label=queue
  name: Merge priority high
queue_rules:
- conditions:
  - check-success=continuous-integration/fake-ci
  name: default
