pull_request_rules:
- actions:
    queue:
      name: default
      priority: high
  conditions:
  - base=20211110112532/test_batch_split_queue/main
  - label=queue
  name: Merge priority high
queue_rules:
- batch_size: 3
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 2
