merge_queue:
  max_parallel_checks: 1
pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20250929140434/test_queue_with_rebase_update_method/main
  - label=queue
  name: default merge
queue_rules:
- autosquash: false
  name: default
  queue_conditions:
  - status-success=continuous-integration/fake-ci
  update_bot_account: '{% if 1 -%}mergify-test4{% endif -%}'
  update_method: rebase
