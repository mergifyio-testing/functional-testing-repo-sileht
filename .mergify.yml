pull_request_rules:
- actions:
    queue:
      name: default
      priority: high
  conditions:
  - status-success=continuous-integration/fake-ci
  - base=20220208155842/test_queue_auto_reembark/main
  name: Merge priority high
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 1
