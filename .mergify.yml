pull_request_rules:
- actions:
    queue:
      rebase_fallback: merge
  conditions:
  - base=20230307134416/test_rebase_fallback_brownout/main
  - label=high
  - status-success=continuous-integration/fake-ci
  name: Merge priority high
