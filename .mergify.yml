pull_request_rules:
- actions:
    queue:
      rebase_fallback: merge
  conditions:
  - base=20230307135237/test_rebase_fallback_deprecation_notice/main
  - label=high
  - status-success=continuous-integration/fake-ci
  name: Merge priority high
