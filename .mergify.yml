pull_request_rules:
- actions:
    queue:
      name: default
      priority: high
  conditions:
  - base=20211022055210/test_batch_queue/main
  - label=queue
  name: Merge priority high
queue_rules:
- batch_size: 2
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 2
