pull_request_rules:
- actions:
    queue:
      name: default
      priority: high
  conditions:
  - base=20210824140429/test_queue_ci_failure/main
  - label=queue
  name: Merge priority high
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 5
