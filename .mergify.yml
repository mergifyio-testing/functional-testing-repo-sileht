pull_request_rules:
- actions:
    queue:
      name: default
      priority: high
  conditions:
  - base=20211215081150/test_queue_inplace/main
  - status-success=slow-ci
  - label=queue
  name: Merge priority high
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 5
