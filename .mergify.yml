merge_queue:
  max_parallel_checks: 5
queue_rules:
- batch_max_wait_time: 0 s
  batch_size: 2
  merge_conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  queue_conditions:
  - base=20260617112025/test_monorepo_scope_lifecycle/main
scopes:
  source:
    files:
      backend:
        include:
        - backend/**
      frontend:
        include:
        - frontend/**
