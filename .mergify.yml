pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20220107170336/test_queue_rule_deleted/main
  name: Merge me
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 5
