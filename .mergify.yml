pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20220831092600/test_queue_with_allow_queue_branch_edit/main
  name: Merge priority high
queue_rules:
- allow_inplace_checks: false
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
