merge_queue:
  max_parallel_checks: 1
pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20251017142600/test_queue_inplace_autosquash_false/main
  - label=queue
  name: Automatic merge
queue_rules:
- autosquash: false
  name: default
  queue_conditions:
  - status-success=continuous-integration/fake-ci
  update_bot_account: mergify-test4
  update_method: rebase
