merge_queue:
  max_parallel_checks: 1
queue_rules:
- autoqueue: true
  name: default
  queue_conditions:
  - base=20260415092357/test_inplace_checks_failure_and_synchronize/main
  - label=queue
  - check-success=continuous-integration/fake-ci
  update_method: rebase
