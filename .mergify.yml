pull_request_rules:
- actions:
    label:
      toggle:
      - CI:fail
  conditions:
  - base=20221122143911/test_label_toggle/main
  - check-failure=continuous-integration/fake-ci
  name: toggle labels
