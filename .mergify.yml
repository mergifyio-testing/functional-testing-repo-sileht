pull_request_rules:
- actions:
    queue:
      name: default
      priority: high
  conditions:
  - base=20220502150031/test_queue_with_labels/main
  - label=queue
  name: Merge priority high
queue_rules:
- allow_inplace_checks: true
  conditions:
  - status-success=continuous-integration/fake-ci
  - label=foobar
  name: default
  speculative_checks: 5
