pull_request_rules:
- actions:
    label:
      toggle:
      - CI:fail
  conditions:
  - base=20250905204721/test_label_toggle_after_synchronize/main
  - check-failure=continuous-integration/fake-ci
  name: toggle labels
