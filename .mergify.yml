merge_queue:
  max_parallel_checks: 1
pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20251022080949/test_queue_inplace_uptodate_autosquash_true/main
  - label=queue
  name: Automatic merge
queue_rules:
- autosquash: true
  name: default
  queue_conditions:
  - status-success=continuous-integration/fake-ci
  update_bot_account: mergify-test4
  update_method: rebase
