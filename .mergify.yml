merge_queue:
  max_parallel_checks: 5
pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20251022082009/test_queue_no_tmp_pull_request/main
  - label=queue
  - status-success=continuous-integration/fake-ci
  name: Merge train
queue_rules:
- name: default
