pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20251020173954/test_rebase_pr_in_queue/main
  - label=queue
  name: Automatic merge
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
