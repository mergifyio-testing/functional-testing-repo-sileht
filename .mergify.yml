pull_request_rules:
- actions:
    queue:
      name: default
      priority: high
  conditions:
  - base=20220111204943/test_queue_cant_create_tmp_pull_request/main
  - label=queue
  name: Merge priority high
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 5
