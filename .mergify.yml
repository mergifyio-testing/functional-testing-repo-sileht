pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20251101224714/test_unqueue_command_after_queue_action_and_command/main
  - label=queue
  name: Merge on queue label
queue_rules:
- merge_conditions:
  - check-success=ci
  name: default
