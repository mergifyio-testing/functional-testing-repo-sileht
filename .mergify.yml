pull_request_rules:
- actions:
    queue:
      name: default
      priority: high
  conditions:
  - base=20220124144839/test_batch_cant_create_tmp_pull_request/main
  - label=queue
  name: Merge priority high
queue_rules:
- batch_size: 2
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 1
