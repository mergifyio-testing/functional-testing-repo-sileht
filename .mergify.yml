merge_queue:
  max_parallel_checks: 5
pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20260116081445/test_queue_with_bot_account/main
  - label=queue
  name: default merge
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  draft_bot_account: '{{ ''mergify-test4'' }}'
  name: default
